#ifndef DEVICE_H
#define DEVICE_H

#include "common.h"

 float get_node_viability_score(unsigned int node, unsigned int *atts, unsigned int *atts_orders_offset,
                                          unsigned int *signatures);

#endif